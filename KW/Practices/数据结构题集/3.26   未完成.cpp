/*int sqrt(double A, double p, double e)
{
	if (p*p - A < e)
	{
		return p;
	}
	else
	{
		return sqrt(A, (p + A / p) / 2, e);
	}
}*/

int sqrt(double A, double p, double e)
{
	while (p*p - A >= e)
	{

	}
}